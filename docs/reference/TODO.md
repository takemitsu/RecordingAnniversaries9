# TODO

## 未実装・今後の拡張 🚧

### セキュリティ強化
- [ ] **Server Actionsレート制限の実装**（優先度: Low）
  - 認証済みアプリではリスク低（悪意のある正規ユーザーのみ）
  - 本番デプロイ前に再検討
  - 実装候補:
    - `upstash/ratelimit` + Redis/Vercel KV（本番推奨）
    - カスタムメモリベース実装（開発環境向け）
  - 推奨レート: 10-20 req/min（フォーム送信）、60 req/min（読み取り）

### 機能拡張
- [ ] **カレンダー機能**（実装予定）
  - 記念日＋祝日を可視化するカレンダー表示
  - **祝日データ管理**:
    - 内閣府CSV（https://www8.cao.go.jp/chosei/shukujitsu/syukujitsu.csv）をJSON化
    - `npm run update-holidays`で年1回更新
    - `public/holidays.json`（git管理）
  - **カレンダーコンポーネント（自作）**:
    - 共通コンポーネント: `components/Calendar.tsx`
    - props: `holidays`（必須）、`anniversaries`（オプション）
    - PC: 2×6グリッド（年次カレンダー、12ヶ月）
    - モバイル: 今月・来月の2ヶ月（縦スクロール）
    - ナビゲーション: 年/月の前後移動、今年/今月に戻る
    - 日付セル: 🎌祝日、🎂記念日アイコン
    - アイコンタップでツールチップ表示（祝日名・記念日名）
    - デザイン: 見やすさ最優先、土日色分け（青系/赤系）、ダークモード対応
    - 技術: dayjs（date-fns不要）、Tailwind CSS v4
  - **ページ構成**:
    - `/holidays` - 祝日カレンダー（未ログインOK、誰でも見られる）
    - `/calendar` - カレンダー（ログイン必要、記念日+祝日表示）
  - **工数見積もり**: 8-10時間（アクセシビリティ最小限）

- [ ] **ブラウザプッシュ通知**（検討中→見送り）
  - 記念日のリマインダー通知
  - Service Worker + Push API
  - 優先度: 見送り（実装コスト高、運用負荷高）

### 運用・保守
- [ ] **定期的な `npm audit` 実行**（CI/CDに統合、オプション）
- [ ] **構造化ログ（JSON）を採用**（オプション）
  - pino などのロガー導入（将来的な拡張）
  - ログ解析ツール（Loki, Grafana）との連携を視野に
  - 現状: プレーンテキストログで十分運用可能

